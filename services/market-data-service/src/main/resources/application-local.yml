spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/marketdatadb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

eureka:
  client:
    enabled: false